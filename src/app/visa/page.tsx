import type { Metadata } from "next";
import VisaForm from "./VisaForm";

export const metadata: Metadata = {
  title: "Visa études Italie — JEExpert Forms",
};

export default function VisaPage() {
  return <VisaForm />;
}
